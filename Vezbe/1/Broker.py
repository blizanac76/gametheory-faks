import Agent

class Broker:
    
    def __init__(self):
        id = self.id

    # def broker_init(self, agent1, agent2, context):
        # agentA = agent1
        # agentB = agent2
        # data = context
    def send_message(agent, data):
        message = data

    def add_agent(id):
        Agent.__init__(id)


